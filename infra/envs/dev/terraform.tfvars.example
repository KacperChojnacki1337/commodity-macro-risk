# Copy this file to terraform.tfvars if you want to override defaults.
# Nothing here is required — the defaults work out of the box, and resource
# names are generated automatically (globally-unique), so this file is safe to
# leave untouched when moving to a new Azure trial.

location    = "polandcentral"
name_prefix = "cmdrisk"
