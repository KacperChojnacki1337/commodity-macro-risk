# Normalize line endings to LF in the repository for all text files.
# Prevents noisy CRLF/LF diffs across Windows/Linux (e.g. GitHub Actions runners).
* text=auto eol=lf

# Treat these as binary (never normalize / diff as text).
*.pbix binary
*.png  binary
*.jpg  binary
*.xlsx binary
